# Nixpacks configuration for Railway
[phases.setup]
nixPkgs = ["nodejs-18_x", "python3", "gcc"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm rebuild swisseph"]

[start]
cmd = "npm start"

